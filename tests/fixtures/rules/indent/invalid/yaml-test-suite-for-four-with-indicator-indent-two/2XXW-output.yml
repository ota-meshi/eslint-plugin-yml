# indent/invalid/yaml-test-suite-for-four-with-indicator-indent-two/2XXW-input.yaml
# Sets are represented as a
# Mapping where each key is
# associated with a null value
--- !!set
    ? Mark McGwire
    ? Sammy Sosa
    ? Ken Griff
