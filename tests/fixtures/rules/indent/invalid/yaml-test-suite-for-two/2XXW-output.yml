# indent/invalid/yaml-test-suite-for-two/2XXW-input.yaml
# Sets are represented as a
# Mapping where each key is
# associated with a null value
--- !!set
  ? Mark McGwire
  ? Sammy Sosa
  ? Ken Griff
